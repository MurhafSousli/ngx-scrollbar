import { CssVariablePipe } from './css-variable.pipe';

describe('CssVariablePipe', () => {
  it('create an instance', () => {
    const pipe = new CssVariablePipe();
    expect(pipe).toBeTruthy();
  });
});
